public class Combat{
   // takes in int Death from interface Monster 
   public static monsterAttack(int Death){
      if (){ // monster encountered
		// monster attack pic
		// player loses health
		System.out.println("You've just been attacked.")
      }
      
      if (Death == 0){
		proceed onwards; // probably requires method
	  }
   }
}